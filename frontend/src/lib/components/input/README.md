# input

This folder contains input components — anything that captures user input, from
bare fields to composed widgets. Base fields live at the top level; each input
subsystem (`citation/`, `dropdown/`, `markdown/`, `wikilink/`) lives in a subfolder.

Display components for those same subsystems (rendering markdown, citations)
live elsewhere.
