# collections

This folder contains UI components for displaying collections of items.

- `cards/` — display single item as a card
- `grid/` — display a collection as a grid of cards
- `list/` — display a collection as a list of rows
- `filters/` — UI for narrowing which items show: filter sidebar, chips, drawers.
