"""Scheme plugin modules — one platform per module.

Adding a platform is one module here exporting ``SCHEME``. The registry
auto-discovers those declarations. See ``SchemeSpec`` in
``citation_scheme_specs.py`` for the contract and
``tests/schemes/test_conformance.py`` for the harness every registered scheme
is automatically held to.
"""
